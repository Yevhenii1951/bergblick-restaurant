# 01: Project scaffold, i18n routing and theme

**What to build:** The Astro + TS + Tailwind + daisyUI project is scaffolded; `de`/`ru`/`en` routing renders a placeholder per locale; the daisyUI `mytheme` is applied; typed i18n dictionary + `useI18n` exists.

**Blocked by:** None (can start immediately)

**Status:** ready-for-agent

- [ ] Locale routing under `src/pages/<locale>/` works (de default redirect)
- [ ] `mytheme` from global.css is active on a sample page
- [ ] Typed `src/i18n/<locale>.ts` dictionaries and a lookup hook/helper exists
- [ ] `lang` attribute and per-locale `<html>` set correctly
