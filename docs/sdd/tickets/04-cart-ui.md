# 04: Cart UI (React island) with localStorage persistence

**What to build:** A React `CartProvider` context (localStorage-persisted) plus a cart drawer/panel that lists dishes, adjusts quantities, shows the running total and free-delivery progress, and links to the order form.

**Blocked by:** 03

**Status:** ready-for-agent

- [ ] CartProvider initializes from localStorage and persists on change
- [ ] Cart UI shows dish name, qty controls, line total, and running grand total
- [ ] Free-delivery progress/indicator uses the threshold from takeaway core
- [ ] Cart is usable add-to-cart from the menu section component
