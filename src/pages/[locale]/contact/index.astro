---
import BaseLayout from '../../../layouts/BaseLayout.astro'
import LocationMap from '../../../components/LocationMap.tsx'
import ScrollReveal from '../../../components/effects/ScrollReveal'
import GradientText from '../../../components/effects/GradientText'
import { getDictionary } from '../../../i18n'
import { SITE } from '../../../lib/config'
import type { Locale } from '../../../i18n/config'

export async function getStaticPaths() {
  const locales: Locale[] = ['de', 'ru', 'en']
  return locales.map((locale) => ({ params: { locale } }))
}

const { locale } = Astro.params as { locale: Locale }
const dict = getDictionary(locale)
---

<BaseLayout locale={locale} title={dict.contact.title}>
  <section class="texture-wood bg-base-100">
    <div class="mx-auto max-w-6xl px-6 py-16 md:py-20">
      <ScrollReveal>
        <div class="mb-10">
          <p class="flex items-center gap-2 font-script text-2xl text-primary md:text-3xl">
            {dict.contact.city}
          </p>
          <h1 class="mt-1 text-4xl font-semibold md:text-5xl">
            <GradientText>{dict.contact.title}</GradientText>
          </h1>
          <p class="mt-4 max-w-2xl text-base-content/70">{dict.contact.subtitle}</p>
        </div>
      </ScrollReveal>

      <div class="grid gap-8 lg:grid-cols-2">
        <ScrollReveal delay={120}>
          <section class="h-full rounded-xl border border-base-300 bg-base-100 p-8 shadow-sm">
            <div class="flex items-center gap-3">
              <span class="grid h-11 w-11 place-items-center rounded-lg bg-primary/10 text-neutral"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="size-5"><path d="M20 10c0 6-8 12-8 12s-8-6-8-12a8 8 0 0 1 16 0z"/><circle cx="12" cy="10" r="3"/></svg></span>
              <h2 class="font-display text-xl font-semibold">{dict.contact.address}</h2>
            </div>
            <p class="mt-3 text-base-content/80">{SITE.address.street}</p>
            <p class="text-base-content/80">{SITE.address.city}</p>

            <div class="mt-6 flex items-center gap-3">
              <span class="grid h-11 w-11 place-items-center rounded-lg bg-accent/10 text-neutral"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="size-5"><circle cx="12" cy="12" r="10"/><polyline points="12 6 12 12 16 14"/></svg></span>
              <h2 class="font-display text-xl font-semibold">{dict.contact.hours}</h2>
            </div>
            <p class="mt-3 text-base-content/80">{SITE.hours}</p>

            <div class="mt-6 flex items-center gap-3">
              <span class="grid h-11 w-11 place-items-center rounded-lg bg-secondary/10 text-neutral"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="size-5"><path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6 19.79 19.79 0 0 1-3.07-8.67A2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72c.13.96.36 1.9.7 2.81a2 2 0 0 1-.45 2.11L8.09 9.91a16 16 0 0 0 6 6l1.27-1.27a2 2 0 0 1 2.11-.45c.91.34 1.85.57 2.81.7A2 2 0 0 1 22 16.92z"/></svg></span>
              <h2 class="font-display text-xl font-semibold">{dict.contact.phone}</h2>
            </div>
            <a class="mt-3 inline-block text-lg font-semibold text-primary hover:underline" href={`tel:${SITE.phone}`}>
              {SITE.phone}
            </a>

            <div class="mt-6 flex items-center gap-3">
              <span class="grid h-11 w-11 place-items-center rounded-lg bg-primary/10 text-neutral"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="size-5"><path d="M17 8h1a4 4 0 1 1 0 8h-1"/><path d="M3 8h14v9a4 4 0 0 1-4 4H7a4 4 0 0 1-4-4z"/><line x1="3" y1="12" x2="17" y2="12"/></svg></span>
              <h2 class="font-display text-xl font-semibold">{dict.contact.deliveryTitle}</h2>
            </div>
            <p class="mt-3 text-base-content/80">{dict.contact.deliveryHours}</p>
            <p class="mt-1 text-base-content/80">{dict.contact.freeDelivery}</p>

            <a href={`/${locale}/reservation/`} class="btn btn-outline mt-6 w-full rounded-full border-base-300">
              {dict.contact.reserve}
            </a>
          </section>
        </ScrollReveal>

        <ScrollReveal delay={240} direction="left">
          <LocationMap dict={dict} client:only="react" />
        </ScrollReveal>
      </div>
    </div>
  </section>
</BaseLayout>
